*Done:*
> Server reset reworked to be paced + cancellable: instead of one big backend call, the website now resets players one at a time (reusing /players/:id/reset), shows live "Resetting X/Y — name" progress, paces ~1.5s after each Riot-backed reset to respect the dev-key limit, and has a Cancel button that stops before the next player (already-done ones stay reset). Removed the bulk /players/reset-all endpoint.
> AuthControl QoL: (1) the key box is no longer seeded from the saved key — after switching servers via a magic link it used to hold the OLD key and log you into the wrong server; now an empty box means "the server I'm on" and password-only unlock targets the live scope. (2) Added a "View as player" button (and empty-submit) to drop admin while staying on the server, so you can preview the player view without disconnecting. Panel copy now reflects connected/unlocked state.
> One-click server link: the bot's #info post now shows a magic link `https://<site>/s/<serverKey>` instead of "paste this key". Opening it scopes the website to that server, then auto-strips the key from the address bar (kept in localStorage, sent as X-Server-Key) so it doesn't linger in history/referrers. Admins then just enter the password to unlock. Manual paste still works as a fallback; the key stays rotatable (/setup rotate_key:true). Switching to a different server's link drops the old server's admin unlock (one active scope per browser).
> Owner-only setup fixed + reworded: setting the website password the FIRST time is now owner-only too (was any-admin), matching later changes and key rotation. The server owner runs `/setup password:<...>` once; any other admin can re-run `/setup` (no password) to repair roles/channels. Wording updated across the bot command, error messages, README, DEPLOY, bot README.

*Pending:*
